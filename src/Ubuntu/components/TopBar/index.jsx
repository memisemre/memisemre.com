import DateDisplay from "./DateDisplay";
import { TopBarWrapper } from "./style";

const TopBar = () => {
  return (
    <TopBarWrapper>
      <DateDisplay />
    </TopBarWrapper>
  );
};
export default TopBar;
